package RMI.RMI_LogicMessages.server_to_client;

import RMI.RMI_Classes.*;
import RMI.AutoCreatedClass.*;
import java.util.LinkedList;
public class Logic_initializeWorldMap {

    //Called_RMI(Remote Method Invocation)_Method
    public static void RMI_Packet(RMI_ID rmi_id, short rmi_ctx, LinkedList<CharacterData> characterList, LinkedList<MonsterData> monsterList, LinkedList<BuffTurretData> buffTurretList, LinkedList<AttackTurretData> attackTurretList, LinkedList<BarricadeData> barricadeList, LinkedList<CrystalData> crystalList, LinkedList<SkillObjectData> skillObjectList, LinkedList<FlyingObjectData> flyingObjectList, LinkedList<BuildSlotData> buildSlotList)
    {
        //do something.
    }
}